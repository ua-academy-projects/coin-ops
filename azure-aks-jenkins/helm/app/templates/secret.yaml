apiVersion: v1
kind: Secret
metadata:
  name: {{ include "sample-app.fullname" . }}-secret
type: Opaque
stringData:
{{- range $key, $value := .Values.secret }}
  {{ $key }}: {{ $value | quote }}
{{- end }}

