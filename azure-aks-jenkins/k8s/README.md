# Kubernetes Notes

This directory is reserved for optional cluster-side manifests that are not Helm-managed.

The baseline implementation keeps application delivery inside:

- `helm/app`
- Terraform Helm release for Jenkins

