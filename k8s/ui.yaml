apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui
  namespace: coin-ops
  labels:
    app.kubernetes.io/name: ui
    app.kubernetes.io/part-of: coin-ops
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ui
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ui
        app.kubernetes.io/part-of: coin-ops
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: ui
          image: ghcr.io/ua-academy-projects/coin-ops-ui:shabat-latest
          ports:
            - name: http
              containerPort: 80
          env:
            - name: PROXY_URL
              valueFrom:
                configMapKeyRef:
                  name: coin-ops-config
                  key: PROXY_URL
            - name: HISTORY_URL
              valueFrom:
                configMapKeyRef:
                  name: coin-ops-config
                  key: HISTORY_URL
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: coin-ops
  labels:
    app.kubernetes.io/name: ui
    app.kubernetes.io/part-of: coin-ops
spec:
  selector:
    app.kubernetes.io/name: ui
  ports:
    - name: http
      port: 80
      targetPort: http
