{{- if .Values.history.api.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.history.api.name }}
  namespace: {{ .Values.global.backendNamespace }}
  labels:
    {{- include "coinops.labels" (dict "ctx" . "component" .Values.history.api.name) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "coinops.selectorLabels" (dict "ctx" . "component" .Values.history.api.name) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.history.api.servicePort }}
      targetPort: http
{{- end }}
