{{- if .Values.proxy.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.proxy.name }}
  namespace: {{ include "coin-ops.appNamespace" . }}
  labels:
    {{- include "coin-ops.nameLabels" (dict "root" . "name" .Values.proxy.name) | nindent 4 }}
spec:
  replicas: {{ .Values.proxy.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.proxy.name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.proxy.name }}
        app.kubernetes.io/part-of: {{ include "coin-ops.partOf" . }}
    spec:
      terminationGracePeriodSeconds: {{ .Values.proxy.terminationGracePeriodSeconds }}
      {{- include "coin-ops.imagePullSecrets" . | nindent 6 }}
      containers:
        - name: {{ .Values.proxy.name }}
          image: {{ include "coin-ops.image" .Values.proxy.image }}
          imagePullPolicy: {{ .Values.proxy.image.pullPolicy }}
          securityContext:
{{ toYaml .Values.proxy.securityContext | indent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.proxy.containerPort }}
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ .Values.config.name }}
                  key: PROXY_PORT
          envFrom:
            - secretRef:
                name: {{ .Values.secret.name }}
            - configMapRef:
                name: {{ .Values.config.name }}
          resources:
{{ toYaml .Values.proxy.resources | indent 12 }}
          readinessProbe:
            httpGet:
              path: {{ .Values.proxy.readinessProbe.path }}
              port: http
            initialDelaySeconds: {{ .Values.proxy.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.proxy.readinessProbe.periodSeconds }}
          livenessProbe:
            httpGet:
              path: {{ .Values.proxy.livenessProbe.path }}
              port: http
            initialDelaySeconds: {{ .Values.proxy.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.proxy.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.proxy.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.proxy.livenessProbe.failureThreshold }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.proxy.name }}
  namespace: {{ include "coin-ops.appNamespace" . }}
  labels:
    {{- include "coin-ops.nameLabels" (dict "root" . "name" .Values.proxy.name) | nindent 4 }}
spec:
  selector:
    app.kubernetes.io/name: {{ .Values.proxy.name }}
  ports:
    - name: http
      port: {{ .Values.proxy.service.port }}
      targetPort: http
{{- end }}
