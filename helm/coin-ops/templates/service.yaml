apiVersion: v1
kind: Service
metadata:
  name: {{ include "coin-ops.fullname" . }}
  labels:
{{ include "coin-ops.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "coin-ops.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
