---
- name: Base OS preparation for K3s nodes
  hosts: k3s_servers
  become: true
  pre_tasks:
    - name: Wait for SSH connectivity
      ansible.builtin.wait_for_connection:
        timeout: 300
  roles:
    - k3s_common
