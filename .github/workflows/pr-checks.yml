name: PR Checks

on:
  workflow_dispatch:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

permissions:
  contents: read

jobs:
  frontend:
    name: Frontend
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: services/ui

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: services/ui/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run lint
        run: npm run lint

      - name: Run tests
        run: npm run test:run --if-present

      - name: Build frontend
        run: npm run build

  go:
    name: Go
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: services/proxy

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: services/proxy/go.mod

      - name: Run tests
        run: go test ./...

      - name: Build Go project
        run: go build ./...

  python:
    name: Python
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: services/history

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build custom Postgres image
        working-directory: .
        run: docker build -t coinops-postgres-runtime:latest -f tests/integration/Dockerfile.pg_sql .

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies and Ruff
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt ruff

      - name: Run Ruff
        run: ruff check .

      - name: Check Python syntax
        run: python -m py_compile main.py consumer.py

      - name: Run unit tests
        working-directory: .
        run: python -m pytest tests/unit -v

      - name: Run integration tests
        working-directory: .
        run: python -m pytest tests/integration -v

  docker:
    name: Docker
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - service: proxy
            context: ./services/proxy
            dockerfile: ./services/proxy/Dockerfile
          - service: history-api
            context: ./services/history
            dockerfile: ./services/history/Dockerfile.api
          - service: history-consumer
            context: ./services/history
            dockerfile: ./services/history/Dockerfile.consumer
          - service: ui
            context: ./services/ui
            dockerfile: ./services/ui/Dockerfile
          - service: postgres-runtime
            context: ./services/postgres-runtime
            dockerfile: ./services/postgres-runtime/Dockerfile

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image for ${{ matrix.service }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: false
