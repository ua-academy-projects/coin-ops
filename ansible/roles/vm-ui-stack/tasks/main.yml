---
# Meta-role: provision + deploy the VM-mode UI node.
# Applied to `hosts: ui` by deploy.yml.

- import_role:
    name: common

- import_role:
    name: docker

- import_role:
    name: registry-login

- import_role:
    name: ui
