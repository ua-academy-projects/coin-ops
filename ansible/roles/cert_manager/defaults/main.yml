---
# Default variables for cert-manager role.
# Uses Let's Encrypt staging for testing, switch to prod when ready.
cert_manager_version: "v1.14.0"
cert_manager_email: "marta.penina.academic@gmail.com"
cert_manager_namespace: "cert-manager"
letsencrypt_server: "https://acme-v02.api.letsencrypt.org/directory"