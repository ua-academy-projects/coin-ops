---
- name: Stop and disable legacy host services
  service:
    name: "{{ item }}"
    state: stopped
    enabled: no
  loop:
    - redis-server
    - cognitor-proxy
  failed_when: false

- name: Create secrets directory
  file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: '0750'
  loop:
    - /etc/cognitor
    - /opt/coinops

- name: Write proxy environment file (Bootstrap)
  template:
    src: proxy.env.j2
    dest: /etc/cognitor/proxy.env
    owner: root
    group: root
    mode: '0640'

- name: Write application configuration file
  template:
    src: config.yaml.j2
    dest: /opt/coinops/config.yaml
    owner: root
    group: root
    mode: '0644'

- name: Create proxy deploy directory
  file:
    path: /opt/cognitor/proxy
    state: directory
    owner: root
    group: root
    mode: '0755'

- name: Remove legacy synced proxy source or binary
  file:
    path: /opt/cognitor/proxy/proxy
    state: absent

- name: Render compose file
  template:
    src: "{{ playbook_dir }}/../deploy/compose/node-02.compose.yaml"
    dest: /opt/cognitor/proxy/compose.yaml

- name: Validate proxy compose configuration
  command: docker compose -f /opt/cognitor/proxy/compose.yaml config -q
  args:
    chdir: /opt/cognitor/proxy
  changed_when: false

- name: Repair Redis data directory ownership
  file:
    path: /var/lib/coin-ops/redis
    state: directory
    owner: "999"
    group: "999"
    recurse: yes
  when: runtime_backend == 'external'

- name: Pull proxy container images
  command: docker compose -f /opt/cognitor/proxy/compose.yaml pull
  args:
    chdir: /opt/cognitor/proxy

- name: Run Docker Compose
  command: docker compose -f /opt/cognitor/proxy/compose.yaml up -d --remove-orphans
  args:
    chdir: /opt/cognitor/proxy

- name: Wait for proxy health endpoint
  uri:
    url: "http://localhost:{{ proxy_port }}/health"
    method: GET
    status_code: 200
  retries: 12
  delay: 5
  register: result
  until: result.status == 200

- name: Remove dangling Docker images
  command: docker image prune -f
  changed_when: false
