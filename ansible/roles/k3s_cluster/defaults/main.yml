#SPDX-License-Identifier: MIT-0
---
# defaults file for roles/k3s_cluster

# -> k3s
k3s_cluster_nodes_group: all
# move to tasks
k3s_cluster_primary_node: "{{ groups[k3s_cluster_nodes_group][0] }}"
k3s_cluster_node_ip: private_ip
k3s_cluster_version: "v1.35.5+k3s1"
