apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ coinops_history_api_app_name }}
  namespace: {{ coinops_history_namespace }}
  labels:
    app.kubernetes.io/name: {{ coinops_history_api_app_name }}
spec:
  replicas: {{ coinops_history_api_replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ coinops_history_api_app_name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ coinops_history_api_app_name }}
    spec:
      imagePullSecrets:
        - name: {{ coinops_history_image_pull_secret_name }}
      containers:
        - name: history-api
          image: {{ coinops_history_api_image }}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: {{ coinops_history_api_container_port }}
          env:
            - name: PORT
              value: "{{ coinops_history_api_container_port }}"
            - name: RUNTIME_BACKEND
              value: "{{ coinops_history_runtime_backend }}"
          envFrom:
            - secretRef:
                name: {{ coinops_history_backend_env_secret_name }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
