# vm-proxy-stack

Full lifecycle (system prep + docker install + registry login + service
deploy) for a VM-mode proxy node — Go proxy + Redis.

Applied to `hosts: proxy` by `deploy.yml`.

Imports: [[common]], [[docker]], [[registry-login]], [[proxy]].
