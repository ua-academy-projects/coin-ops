apiVersion: v1
kind: Service
metadata:
  name: {{ coinops_ui_app_name }}
  namespace: {{ coinops_ui_namespace }}
  labels:
    app.kubernetes.io/name: {{ coinops_ui_app_name }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ coinops_ui_app_name }}
  ports:
    - name: http
      port: {{ coinops_ui_service_port }}
      targetPort: http
