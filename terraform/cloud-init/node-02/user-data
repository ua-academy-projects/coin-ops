#cloud-config
users:
  - name: vagrant
    sudo: ALL=(ALL) NOPASSWD:ALL
    shell: /bin/bash
    lock_passwd: false
    ssh_authorized_keys:
      - REPLACE_WITH_SSH_KEY

chpasswd:
  list: |
    vagrant:vagrant
  expire: false

package_update: true
packages:
  - openssh-server
  - python3

runcmd:
  - systemctl enable ssh
  - systemctl start ssh
