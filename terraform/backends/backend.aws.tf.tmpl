terraform {
  backend "s3" {
    bucket       = "__AWS_STATE_BUCKET__"
    key          = "__AWS_STATE_KEY__"
    region       = "__AWS_STATE_REGION__"
    encrypt      = true
    use_lockfile = true
  }
}
