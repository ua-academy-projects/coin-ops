# Repo-root Ansible config. Run `ansible-playbook ...` from the repo root.
# Paths are resolved relative to this file.

[defaults]
roles_path        = ansible/roles
collections_paths = ansible/collections
inventory         = ansible/inventories/gcp-vm
