# Comparative Analysis Matrix — 10 Student Branches

> **Reviewer**: Independent Senior Software Architect / DevOps Lead (Claude Opus 4.6)
> **Date**: 2026-04-13
> **Repository**: `ua-academy-projects/coin-ops`
> **Evaluation context**: Enterprise readiness, 12-Factor compliance, cloud-native migration potential

---

## Legend

| Rating | Meaning |
|--------|---------|
| **High** | Production-grade pattern; minimal rework needed for enterprise deployment |
| **Medium** | Functional and demonstrates understanding; needs hardening for production |
| **Low** | Prototype-level or absent; significant rework required |
| **N/A** | Component not implemented in this branch |

---

## Master Comparison Table

| Branch | Architecture Quality | API Robustness | Observability | Config Management Readiness | Security | Task Completeness |
|:---|:---|:---|:---|:---|:---|:---|
| **Shabat** | **High** — React SPA + Go proxy + Python FastAPI history + separate consumer. 3-node Compose via Ansible. CI/CD with GHCR. Clean bounded contexts. | **Medium-High** — Multiple APIs (Polymarket, CoinGecko, NBU). HTTP client has 10s timeout but lacks retry/backoff for external calls. RabbitMQ dial has infinite retry. | **Medium** — Health endpoints on proxy and nginx. No structured JSON logging, no Prometheus metrics, no distributed tracing. | **High** — Ansible roles with per-host env templating, Terraform for Hyper-V VMs, GHCR image registry, multi-stage Docker builds. Best infra-as-code among all branches. | **Medium-High** — Secrets via env files (root-owned 0640), no hardcoded passwords in source. No Vault integration. CORS permissive. | **High** — All 6 components present (Frontend, Proxy, History, PostgreSQL, Redis, RabbitMQ). Two languages (Go + Python). |
| **hrenchevskyi** | **High** — Flask BFF + Go proxy + Python history service. Modular Go code (`middleware.go`, `http_retry.go`, `config.go`, `publisher.go`). Ansible + Docker Compose hybrid. | **High** — Best-in-class retry logic: exponential backoff with `Retry-After` header parsing, 429/5xx detection. In-memory cache with TTL. Background poll + publish. | **Medium** — Request logging middleware, health endpoints. Uses `log.Printf` / Python `logging` — not structured JSON. No metrics/tracing. | **High** — Strict `config.go` struct mapping with fail-fast on missing vars. Ansible Vault for secrets. `.env.example` with `REDACTED_BY_VAULT` placeholders. 12-Factor exemplary. | **High** — Ansible Vault for secrets, .env.example with redacted values, CSP headers, rate limiting on frontend, security headers middleware. Best security posture. | **High** — All 6 components present. Redis for UI state, RabbitMQ with durable/persistent/ACK-after-commit. Two languages (Go + Python). |
| **kazachuk** | **Medium** — Standard Go proxy + Python Flask frontend + Python consumer. Dockerized with multi-stage builds. Clear directory separation. | **Medium** — Basic timeouts (5-10s). No retry/backoff for external APIs. No handling of 429 rate limits from CoinGecko. | **Low-Medium** — `log` in Go, `print` in Python. No health endpoints, no metrics, no structured logs. | **Medium** — Docker Compose with env vars. Passwords like `coinops123` hardcoded in compose and code defaults. No Ansible Vault. | **Low-Medium** — Secrets in plaintext in compose/code. No security headers. No rate limiting. CORS not configured. | **High** — All 6 components (Frontend, Proxy, History, PostgreSQL, Redis for favorites, RabbitMQ). Two languages (Go + Python). |
| **kurdupel** | **Medium** — Python Flask proxy + Go history service. Vagrant + Ansible. Proper VM isolation. Rich history API (stats, charts, downsampling). | **Medium** — Coinbase API with 5s timeout. No retry/backoff. Smoothing algorithm for price spikes is a nice touch but not resilient to API failures. | **Low** — Only `print`/`log` statements. No health endpoints. No metrics or structured logging. | **Low-Medium** — Env vars with hardcoded IP defaults (192.168.56.x). Passwords in Ansible plaintext. SSH key paths hardcoded to developer's machine. | **Low** — Passwords in code and Ansible. Committed binary (~8.8MB) in git. `protected-mode no` on Redis. Inventory with local paths. | **Medium** — All components present. Critical routing bug in proxy (`@app.route("/price/ ")` mismatch). Committed binary is a hygiene issue. |
| **monero-privacy-system** | **High** — Clean domain model (Monero privacy analytics). Well-structured FastAPI + worker + core modules. Terraform + cloud-init for 3 VMs. | **Medium-High** — `tenacity` retries (3 attempts, exponential backoff) for Monero RPC and CoinGecko. `httpx` with explicit timeouts (30s/15s). Best retry lib usage. | **Medium** — `structlog` for structured logging (best logging library choice). Health endpoint with Docker HEALTHCHECK. No metrics/tracing. | **Medium-High** — `pydantic-settings` for config. `deploy.env.template` files. Docker Compose for local dev. No Ansible Vault. | **Medium** — Dev passwords in compose. CORS `*` + `allow_credentials=True` (invalid combination). No Vault. Template files for secrets. | **Low** — Missing Redis, RabbitMQ, separate proxy. No compiled language service. Worker uses asyncio loop, not message queue. Does not meet task architecture. |
| **penina** | **Medium** — React (Vite) + Python Flask proxy + Go consumer. Docker + Ansible. Good data flow concept. | **Low-Medium** — 5s timeout only. No retry. `str(dict)` serialization instead of `json.dumps()` creates parsing nightmare downstream. | **Low** — Only `print`/`fmt`. No health checks in compose for app services. No structured logging. | **Low-Medium** — Some env vars in compose. Hardcoded IPs in frontend code. Docker build paths misaligned with repo structure (builds will fail). | **Low-Medium** — Passwords exposed in compose, code, and Ansible playbooks. RabbitMQ definitions.json has separate ACL users (positive). | **Medium-High** — All 6 components present. But Docker compose won't build due to path mismatches. `str(dict)` serialization is a data integrity issue. |
| **shturyn** | **Medium** — React 19 + TypeScript + Go backend + Python FastAPI history. Clean separation. Vite + nginx multi-stage Docker. | **Medium** — Open-Meteo API. Go backend has 3s timeout for history service call. No timeout for external API call. No retry logic. | **Low** — `log`/`print` only. No health endpoints. No healthchecks in compose. No metrics. | **Medium** — Env vars for passwords (not hardcoded in source). No `.env.example`. `FLASK_DEBUG` absent (good). Some URLs hardcoded in Go. | **Medium** — Secrets not in source code (via env). CORS `*` on both services. No input validation on lat/lon params. | **Medium** — Missing Redis entirely. Bug: HTTPException caught by generic except → returns 500 instead of 404. auto_ack in consumer. Only 4 commits. |
| **smoliakov** | **Medium** — Django + Gunicorn + nginx + Go collector + Python consumer. 3 VMs via Vagrant + Ansible. Terraform for AWS EC2. | **Medium** — HTTP timeout 15s with context. `PublishWithContext` with 10s timeout. No retry for external API calls. | **Medium** — Go collector has `/healthz` endpoint with JSON status (best health check among all branches). Python `logging`. No metrics. | **Medium** — Many env vars via systemd `Environment=`. But passwords hardcoded in Ansible (`StrongPassword123!`). Terraform with AWS but SSH open to 0.0.0.0/0. | **Low** — Passwords in Ansible and code. Open pg_hba (`0.0.0.0/0 md5`). SSH from anywhere in Terraform SG. Shebang typo in consumer. Committed binary. | **High** — All 6 components present (Django with Redis cache, Go collector, RabbitMQ, PostgreSQL). Terraform for cloud. Two languages. |
| **volynets** | **N/A** — Only `LICENSE` file exists. No application code, no infrastructure, no documentation. | **N/A** | **N/A** | **N/A** | **N/A** — No attack surface (no code). | **N/A** — Branch contains only initial commit with LICENSE. No implementation delivered. |
| **zakipnyi** | **Medium-High** — Static HTML/JS + FastAPI + Go collector + Python consumer. 5 VMs via Vagrant. Clean role separation. Fanout exchange pattern. | **Medium** — No HTTP timeouts in Go collector. Fanout exchange is appropriate for broadcast pattern. 30 retry attempts for RabbitMQ connection. | **Low** — Standard `logging`/`log`. No health endpoints. No metrics. No structured logs. | **Low-Medium** — Env vars with fallback defaults containing passwords (`coinops123`). No `.env.example`. Vagrant provisioning scripts. | **Low** — Passwords hardcoded in defaults, README, Vagrant. No TLS. CORS `*`. No input sanitization beyond basic `upper()`. | **Medium-High** — Missing Redis. Otherwise all components present (Frontend, nginx proxy, FastAPI, Go collector, RabbitMQ fanout, PostgreSQL). Two languages. |

---

## Ranking Summary (Weighted by Enterprise Readiness)

| Rank | Branch | Strengths | Critical Gaps |
|:---:|:---|:---|:---|
| 1 | **hrenchevskyi** | Best API resilience, best security (Vault), best config management, idempotent DB writes, ACK-after-commit | Observability (no metrics), FLASK_DEBUG=1, basic_get polling |
| 2 | **Shabat** | Best infrastructure (Ansible+Terraform+CI), React SPA with runtime injection, all components, GHCR pipeline | No HTTP retry for external APIs, Terraform locked to Hyper-V, whale tables unused |
| 3 | **monero-privacy-system** | Best logging (structlog), best retry library (tenacity), cleanest domain model, Terraform+cloud-init | Missing Redis/RabbitMQ/proxy, no compiled language service, doesn't meet task architecture |
| 4 | **smoliakov** | All components including Redis cache, Terraform for AWS, health endpoint on collector | Hardcoded passwords, open pg_hba, committed binaries, broken sparkline UI |
| 5 | **kazachuk** | All components present, Docker multi-stage, working data flow | Non-durable queue, auto_ack, hardcoded passwords, no health/logging |
| 6 | **zakipnyi** | Clean 5-VM architecture, fanout exchange, FastAPI+Go, good SQL schema | Missing Redis, hardcoded passwords, no health endpoints, Go reconnect leak |
| 7 | **shturyn** | TypeScript React, multi-stage Docker, clean Go/Python split | Missing Redis, HTTPException bug, auto_ack, only 4 commits |
| 8 | **kurdupel** | Rich history API (stats/charts), Vagrant+Ansible, Go history service | Critical proxy routing bug, committed binary, hardcoded passwords/paths |
| 9 | **penina** | All components present, RabbitMQ ACL definitions, React+Vite UI | Docker builds broken, str(dict) serialization, auto_ack, hardcoded IPs |
| 10 | **volynets** | Clean LICENSE | No implementation delivered |
